//! `sciencekit` — Machine Learning library in Rust reimplementing all of
//! scikit-learn, with extreme performance, zero-copy and native out-of-core.
//!
//! This crate is the umbrella re-export of the `sciencekit_*` sub-crates. It is
//! currently empty: the sub-crates arrive one per change under `crates/` (PRD
//! §3.1) and are re-exported here once they land.

#![forbid(unsafe_code)]
#![warn(missing_docs)]
